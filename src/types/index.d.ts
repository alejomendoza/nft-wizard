export type User = {
  account_id: string;
};

export type Metadata = {
  name: string;
  code: string;
  issuer: string;
  domain: string;
  description: string;
  hash: string;
  cid: string;
  url: string;
};
